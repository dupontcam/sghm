# Backend Environment Variables (Template)
# Copie este arquivo para .env e configure as variáveis

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/sghm_db

# JWT Configuration
# IMPORTANTE: Gerar secrets seguros e únicos (32+ caracteres)
JWT_SECRET=seu_jwt_secret_minimo_32_caracteres_aqui
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=seu_refresh_secret_diferente_32_chars_aqui
JWT_REFRESH_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (produção)
# CORS_ORIGIN=https://seu-dominio.vercel.app
